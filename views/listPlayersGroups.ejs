<%- include('header') %>
<h2>Game board</h2>
<div class="bg-danger"><%= errorMessage %></div>

<a href="/listPlayers" class="btn btn-info">&lt;&lt; to the people table</a></br></br>

<div class="row border1">
<% groups.forEach(function(group, key) { %>

    <% if (isEditMode) { %>
    <a href="enterResult?teamAId=<%= group.teamsIds[0] %>&teamBId=<%= group.teamsIds[1] %>">
    <% } %>
    <div class="col-sm-4 rounded text-center">
        <span class="clearfix">Field #<%= key+1 %></span>
        <span class="rounded-no-border <%= ((group[0].isWinner) ? 'bg-success' : '') %> <%= ((group[0].isLooser) ? 'bg-danger' : '') %>">
            <%= group[0].firstName %> <%= group[0].lastName[0] %>. <%= group[0].points %>
        </span>
        VS
        <span class="rounded-no-border <%= ((group[1].isWinner) ? 'bg-success' : '') %> <%= ((group[1].isLooser) ? 'bg-danger' : '') %>">
            <%= group[1].firstName %> <%= group[1].lastName[0] %>. <%= group[1].points %>
        </span>
    </div>
    <% if (isEditMode) { %>
    </a>
    <% } %>

<% }); %>

</div>
<%- include('footer') %>